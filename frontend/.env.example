# Objetivo del proxy de Vite en desarrollo (FastAPI local).
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# Solo necesario en `npm run build` / preview en producción: URL pública del API.
# VITE_API_URL=https://tu-api.example.com
